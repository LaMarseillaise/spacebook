<div class="row">
  <% random_photos(@user).each do |photo| %>
    <div class="col-xs-4">
      <div class="panel">
        <%= link_to (image_tag photo.photo.url(:medium), class: "img-responsive img-thumbnail"), photo %>
      </div>
    </div>
  <% end %>
</div>
